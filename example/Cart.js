import React,{ Component } from 'react'
import {
    ScrollView,
    Text,
    StyleSheet,
    View,
    TouchableOpacity,
    Button
} from 'react-native'
import Card from './card'
export default class Cart extends Component{
    render(){
        return(
            <ScrollView>
                
            </ScrollView>
        )
    }
}